version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: fieldio_postgres
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-fieldio}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - fieldio_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:alpine
    container_name: fieldio_redis
    command: redis-server --requirepass ${REDIS_PASSWORD:-fieldio_dev_redis}
    ports:
      - "127.0.0.1:6380:6379"
    volumes:
      - redis_data:/data
    networks:
      - fieldio_network
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-fieldio_dev_redis}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  adminer:
    image: adminer
    container_name: fieldio_adminer
    ports:
      - "127.0.0.1:8080:8080"
    profiles:
      - dev
    networks:
      - fieldio_network
    depends_on:
      - postgres

volumes:
  postgres_data:
  redis_data:

networks:
  fieldio_network:
    driver: bridge
